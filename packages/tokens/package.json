{
  "name": "@pinpoint/tokens",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "comment": "dependencies is intentionally absent, and so is any runtime code. This package holds platform-neutral literals and nothing else — the single source of truth the styling spec requires. Both applications import the same values and apply them in their own idiom; nothing here knows what a stylesheet is. Adding a dependency here means something other than a token value has been put in it.",
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
