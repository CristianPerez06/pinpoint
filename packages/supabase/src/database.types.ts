/**
 * Generated database types.
 *
 * This file is a placeholder. No schema exists yet — the change that first
 * defines one replaces this with the output of the Supabase CLI type
 * generator, and adds the generation command to the root scripts. Until then
 * the client is typed as an empty schema so consumers already import from the
 * right place and gain types without changing a call site.
 */
export interface Database {
  public: {
    Tables: Record<string, never>
    Views: Record<string, never>
    Functions: Record<string, never>
    Enums: Record<string, never>
    CompositeTypes: Record<string, never>
  }
}
