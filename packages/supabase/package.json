{
  "name": "@pinpoint/supabase",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "comment": "Depends only on the isomorphic client. Platform session persistence (cookies on web, secure storage on native) is wired by each app and deliberately does not live here.",
  "dependencies": {
    "@supabase/supabase-js": "^2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
