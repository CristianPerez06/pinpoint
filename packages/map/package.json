{
  "name": "@pinpoint/map",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "comment": "dependencies is intentionally absent. This package sits at the base of the workspace graph and must stay renderer-agnostic so the same logic drives maplibre-gl on web and maplibre-react-native on native. Adding a runtime dependency here is a signal the code belongs in an app instead — see openspec/specs/monorepo-structure.",
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
