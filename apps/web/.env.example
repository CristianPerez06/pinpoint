# Copy to .env.local and fill in. Every variable here is required — the app
# refuses to start without it and names the one that is missing.
#
# Everything below is PUBLISHABLE: Next inlines NEXT_PUBLIC_* into the browser
# bundle, so these values ship to anyone who loads the site. That is fine for
# the two below and fatal for a service_role key — never give one this prefix.

NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_YOUR_KEY_HERE
