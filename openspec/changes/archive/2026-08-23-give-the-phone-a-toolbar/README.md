# give-the-phone-a-toolbar

Replace the phone's row of four text pills with three icon buttons — Search, Drop, Filter. `Clear` moves inside the filter sheet and the Filter button carries the narrowed state. Trips move onto the trip name in the header, and gain archiving.
