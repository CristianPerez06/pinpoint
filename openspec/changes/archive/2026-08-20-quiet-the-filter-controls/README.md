# quiet-the-filter-controls

Stop the filter controls announcing themselves: no count, no names on the closed control, and a permanent Clear whose enabled state declares the narrowing. Web's toolbar splits into a narrow row and an add row; mobile drops its narrowed strip.
