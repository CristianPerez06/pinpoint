# say-what-a-write-is-doing

Make every write in both applications say something while it is happening, refuse to be fired twice, and report its refusal. Records the rule that decides between showing the outcome at once and waiting visibly, and retires the one shared `busy` flag that could not survive a second caller.
