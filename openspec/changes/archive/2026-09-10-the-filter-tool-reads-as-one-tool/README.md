# the-filter-tool-reads-as-one-tool

The web bottom bar's filter trigger takes the same shape as the phone's filter tool: a glyph over a single-line word, the narrowed state badged on the glyph, no count.
