# zoom-from-a-visible-control

Give both maps a visible pair of zoom buttons, so zoom is a control you can see and press rather than a gesture you have to already know. Binds the shared `MIN_ZOOM`/`MAX_ZOOM` to both renderers so every instrument agrees where the range ends.
